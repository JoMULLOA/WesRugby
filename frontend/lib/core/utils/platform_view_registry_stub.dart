/// Stub para plataformas que no son web.
/// No hace nada para que el código compile en todas las plataformas.
void registerViewFactory(String viewType, dynamic Function(int) factory) {
  // no-op
}
